@inject AppUser AppUser
@*@inject IIdentityService IdentityService*@
@*@inject IMatToaster Toaster*@
@inject NavigationManager NavigationManager


    <MatNavMenu>
        <MatNavItem Href="/"><MatIcon Icon="home"></MatIcon>&nbsp; Home</MatNavItem>
        <MatDivider></MatDivider>

        <MatNavItem Href="/"><MatIcon Icon="dashboard"></MatIcon>&nbsp; Dashboard</MatNavItem>
        <MatDivider></MatDivider>

        @if (AppUser.IsSuperAdmin)
        {
            <NavItemsSuperAdmin />
        }

        @if (AppUser.IsAdmin)
        {
            <NavItemsSuperAdmin />
        }

        @if (AppUser.IsSchoolAdmin)
        {
            <NavItemsSchoolAdmin />
        }

        @if (AppUser.IsAssistantSchoolAdmin)
        {
            @*<NavItemsSchoolAdmin />*@
        }

        @if (AppUser.IsTeacher)
        {
            <NavItemsTeacher />
        }

        @if (AppUser.IsParent)
        {
            @*<NavItemsSchoolAdmin />*@
        }

        @if (AppUser.IsStudent)
        {
            <NavItemsStudent />
        }

    <MatNavSubMenu>
        <MatNavSubMenuHeader>
            <MatNavItem AllowSelection="false"><MatIcon Icon="settings"></MatIcon>&nbsp; Settings</MatNavItem>
        </MatNavSubMenuHeader>
        <MatDivider></MatDivider>

        <MatNavSubMenuList>
            <MatNavItem Href="profile">Profile</MatNavItem>
            <MatNavItem Href="change-password">Change Password</MatNavItem>
        </MatNavSubMenuList>
    </MatNavSubMenu>
    </MatNavMenu>


